{
  "name": "@red-requester/ui",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "red-requester UI — SvelteKit (static) client surface.",
  "scripts": {
    "dev": "vite dev --port 1420 --strictPort",
    "build": "vite build",
    "preview": "vite preview",
    "check": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json",
    "test": "echo \"(ui: no unit tests yet)\" && exit 0"
  },
  "dependencies": {
    "@red-requester/core": "workspace:*",
    "@tauri-apps/api": "^2.5.0",
    "yaml": "^2.6.1"
  },
  "devDependencies": {
    "@sveltejs/adapter-static": "^3.0.8",
    "@sveltejs/kit": "^2.17.0",
    "@sveltejs/vite-plugin-svelte": "^5.0.0",
    "@tailwindcss/vite": "^4.0.0",
    "svelte": "^5.20.0",
    "svelte-check": "^4.1.0",
    "tailwindcss": "^4.0.0",
    "typescript": "^5.7.2",
    "vite": "^6.0.0"
  }
}

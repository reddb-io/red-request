---
"@red-request/core": patch
---

UI + release polish: full shadcn-svelte component migration (themed to the red brand), a body/response code editor (line numbers, current-line highlight, Prettify, Copy), drag-to-reorder query/header rows, a unified **Params** tab (Query + Path sub-sections) where path/query/header values accept `{{vars}}`, a project-settings cog (rename / forget / delete data), and the release pipeline (CI, changesets→tag→release, install script, sexy README). Switched git hooks to lefthook.
